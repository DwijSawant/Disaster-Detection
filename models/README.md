# Models Directory

Place your `.pth` (PyTorch) model files in this directory.

### Usage in Next.js
To use these models, you will need to:
1. Convert them to **ONNX** format for browser/Node.js execution.
2. Or use a Python bridge (Flask/FastAPI) to load them.

Path: `/models/`
